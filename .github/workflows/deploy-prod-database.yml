name: Deploy Prod Database

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  terraform-apply-prod-database:
    runs-on: ubuntu-latest
    environment: production
    env:
      AWS_REGION: ${{ vars.AWS_REGION }}
      DB_PROJECT_NAME: ${{ vars.DB_PROJECT_NAME }}
      PROD_DB_VPC_ID: ${{ vars.PROD_DB_VPC_ID }}
      PROD_DB_PRIVATE_SUBNET_IDS: ${{ vars.PROD_DB_PRIVATE_SUBNET_IDS }}
      PROD_DB_ALLOWED_CIDRS: ${{ vars.PROD_DB_ALLOWED_CIDRS }}
      PROD_DB_USERNAME: ${{ vars.PROD_DB_USERNAME }}
      PROD_DB_PASSWORD: ${{ secrets.PROD_DB_PASSWORD }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Validate required database variables
        shell: bash
        run: |
          set -euo pipefail
          if [ -z "${AWS_REGION}" ]; then
            echo "Missing vars.AWS_REGION"
            exit 1
          fi
          if [ -z "${PROD_DB_VPC_ID}" ]; then
            echo "Missing vars.PROD_DB_VPC_ID"
            exit 1
          fi
          if [ -z "${PROD_DB_PRIVATE_SUBNET_IDS}" ]; then
            echo "Missing vars.PROD_DB_PRIVATE_SUBNET_IDS (example: [\"subnet-a\",\"subnet-b\"])"
            exit 1
          fi
          if [ -z "${PROD_DB_PASSWORD}" ]; then
            echo "Missing secrets.PROD_DB_PASSWORD"
            exit 1
          fi

      - name: Create Terraform variable file
        shell: bash
        working-directory: terraform/environments/prod-database
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          import os
          from pathlib import Path

          def parse_json_list(raw_value: str, default_value):
              value = (raw_value or "").strip()
              if not value:
                  return default_value
              parsed = json.loads(value)
              if not isinstance(parsed, list):
                  raise ValueError(f"Expected JSON array, got: {value}")
              return parsed

          data = {
              "aws_region": os.environ["AWS_REGION"],
              "project_name": (os.environ.get("DB_PROJECT_NAME") or "hospital-mgmt").strip(),
              "vpc_id": os.environ["PROD_DB_VPC_ID"],
              "private_subnet_ids": parse_json_list(os.environ.get("PROD_DB_PRIVATE_SUBNET_IDS"), []),
              "allowed_cidr_blocks": parse_json_list(os.environ.get("PROD_DB_ALLOWED_CIDRS"), ["10.30.0.0/16"]),
              "db_username": (os.environ.get("PROD_DB_USERNAME") or "adminuser").strip(),
              "db_password": os.environ["PROD_DB_PASSWORD"],
          }

          Path("github.auto.tfvars.json").write_text(json.dumps(data, indent=2) + "\n", encoding="utf-8")
          PY

      - name: Terraform init (prod-database)
        working-directory: terraform/environments/prod-database
        env:
          TF_STATE_BUCKET: ${{ secrets.TF_STATE_BUCKET }}
          TF_LOCK_TABLE: ${{ secrets.TF_LOCK_TABLE }}
          TF_STATE_KEY: hospital-mgmt/prod-database/terraform.tfstate
        run: |
          if [ -n "$TF_STATE_BUCKET" ] && [ -n "$TF_LOCK_TABLE" ]; then
            terraform init \
              -backend-config="bucket=$TF_STATE_BUCKET" \
              -backend-config="key=$TF_STATE_KEY" \
              -backend-config="region=${{ env.AWS_REGION }}" \
              -backend-config="dynamodb_table=$TF_LOCK_TABLE" \
              -backend-config="encrypt=true"
          else
            terraform init
          fi

      - name: Terraform plan (prod-database)
        working-directory: terraform/environments/prod-database
        run: terraform plan

      - name: Terraform apply (prod-database)
        working-directory: terraform/environments/prod-database
        run: terraform apply -auto-approve

      - name: Show database outputs
        working-directory: terraform/environments/prod-database
        run: terraform output

      - name: Capture handoff values
        id: handoff
        shell: bash
        working-directory: terraform/environments/prod-database
        run: |
          set -euo pipefail
          ENDPOINT="$(terraform output -raw sqlserver_endpoint)"
          PORT="$(terraform output -raw sqlserver_port)"
          IDENTIFIER="$(terraform output -raw sqlserver_identifier)"

          echo "endpoint=$ENDPOINT" >> "$GITHUB_OUTPUT"
          echo "port=$PORT" >> "$GITHUB_OUTPUT"
          echo "identifier=$IDENTIFIER" >> "$GITHUB_OUTPUT"

      - name: Publish hm-database handoff summary
        shell: bash
        run: |
          cat <<EOF >> "$GITHUB_STEP_SUMMARY"
          ## Prod Database Handoff

          Terraform successfully applied the prod SQL Server stack.

          - RDS Identifier: ${{ steps.handoff.outputs.identifier }}
          - Endpoint: ${{ steps.handoff.outputs.endpoint }}
          - Port: ${{ steps.handoff.outputs.port }}
          - Username: ${{ env.PROD_DB_USERNAME || 'adminuser' }}

          Update hm-database repository environment **prod** secrets with:

          - DB_SERVER = ${{ steps.handoff.outputs.endpoint }},${{ steps.handoff.outputs.port }}
          - DB_USERNAME = ${{ env.PROD_DB_USERNAME || 'adminuser' }}
          - DB_PASSWORD = (same value as PROD_DB_PASSWORD secret from this repo)
          EOF

          echo "::notice title=hm-database handoff::Use endpoint ${{ steps.handoff.outputs.endpoint }}:${{ steps.handoff.outputs.port }} in hm-database prod secrets."
